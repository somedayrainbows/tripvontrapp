<h1>All Experiences</h1>

<%  @experiences.each do |experience| %>
  <p>Experience: <%= experience.title %></p>
  <p>Cost: <%= experience.cost %></p>
  <%= button_to "Add to cart", carts_path(experience_id: experience.id) %>
<% end %>
